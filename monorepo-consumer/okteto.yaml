name: monorepo-consumer
command: make serve
sync:
  - .:/src
image:
  name: okteto.dev/monorepo-consumer:${OKTETO_NAMESPACE}
  context: .
  dockerfile: Dockerfile
  target: dev
resources:
  requests:
    cpu: "2"
    memory: 4Gi
persistentVolume:
  enabled: true
  storageClass: standard
  size: 20Gi